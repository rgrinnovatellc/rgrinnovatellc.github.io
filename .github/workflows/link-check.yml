name: Link Check

on:
  push:
    branches: ["master", "main"]
  pull_request:
    branches: ["master", "main"]

jobs:
  links:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Ruby
        uses: ruby/setup-ruby@086ffb1a2090c870a3f881cc91ea83aa4243d408 #1.95.1
        with:
          ruby-version: '3.2'
          bundler-cache: true

      - name: Build site
        run: make build

      - name: Check links
        uses: lycheeverse/lychee-action@v2
        with:
          fail: true
          args: >-
            --verbose
            --no-progress
            --accept 200,206,429
            --max-retries 2
            --max-redirects 20
            --timeout 20
            --root-dir _site
            --exclude "^http://127.0.0.1"
            --exclude "^http://localhost"
            --exclude "^https://www.viperdesignllc.com/?$"
            --exclude "^https://www\.linkedin\.com/in/uddhavpgautam/?$"
            --exclude "^https://doi\.org/"
            --exclude "^http://dx\.doi\.org/"
            --exclude "^https://dl\.acm\.org/"
            --exclude "^http://www\.w3\.org/2005/Atom"
            --exclude "^http://www\.sitemaps\.org/"
            --exclude "\.git$"
            "_site/**/*.html"
            "_site/sitemap.xml"
            "_site/feed.xml"
